import React from "react"


function RandomMeme(props) {
    console.log(props)
    return (
        <div>
            <image>{props.url}</image>
        </div>
    )
}

export default RandomMeme